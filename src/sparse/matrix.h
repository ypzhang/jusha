#pragma once
#include "array.h"

namespace jusha {

  template <typename T>
  class Matrix {
  public:
    
  private:
    
  };

  // utilities:
  template <typename T>
    void AXPY(const T alpha, const Matrix<T> &x, const T beta, cuda::MirroredArray &y)
}


cmake_minimum_required(VERSION 2.8)
project(your_project_name)
set (your_project_name_VERSION 0.1.0)
find_package(CUDA REQUIRED)

# CUDA architecture options, choose what you want
set(GENCODE_SM30 "-gencode=arch=compute_30,code=sm_30")
set(GENCODE_SM35 "-gencode=arch=compute_35,code=sm_35")

set(CUDA_NVCC_FLAGS ${CUDA_NVCC_FLAGS} ${GENCODE_SM30} ${GENCODE_SM35})

# Add the base directory to your include file search path
include_directories(${CMAKE_SRC_DIR})

set(CPP_SRC
# a list of your cpp files, e.g. file1.cpp file2.cpp
)
set(CU_SRC
# a list of your cu files,  e.g. kernel1.cu kernel2.cu
)
# Headers are here just so CMake will put them in project files.
set(HEADERS
# a list of header files, e.g. header1.h header2.h
)

#if it is a shared library:
cuda_add_library(your_project_name SHARED ${CU_SRC} ${CPP_SRC} ${HEADERS})
# or if it is an executable
# cuda_add_executable(your_project_name ${CU_SRC} ${CPP_SRC} ${HEADERS})


